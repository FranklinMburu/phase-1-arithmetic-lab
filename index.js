
// Write your code here
// Define the numbers for the `multiply` test
const num1 = 31;
const num2 = 2;

// The product of num1 and num2
const multiply = num1 * num2;

// Generate a random integer greater than 0 for the `random` test
const random = Math.floor(Math.random() * 100) + 1;

// Define the numbers for the `mod` test
const num3 = 14;
const num4 = 5;

// Calculate the remainder
const mod = num3 % num4;

// Find the maximum number in a set for the `max` test
const max = Math.max(5, 10, 15, 20);

// Export the variables and functions (if necessary)
module.exports = { num1, num2, multiply, random, num3, num4, mod, max };
